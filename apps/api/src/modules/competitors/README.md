# competitors module
Copy the pattern from `modules/ideas`: `competitors.schema.ts` (zod, imported from @fi/contracts where shared) -> `competitors.repo.ts` (SQL) -> `competitors.service.ts` (rules) -> `competitors.routes.ts` (HTTP).
Mount it in `src/app.ts`. Owner: see .github/CODEOWNERS and docs/team/squads.md.
