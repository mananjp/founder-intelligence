# assumptions module

Copy the pattern from `modules/ideas`: `assumptions.schema.ts` (zod, imported from @fi/contracts where shared) -> `assumptions.repo.ts` (SQL) -> `assumptions.service.ts` (rules) -> `assumptions.routes.ts` (HTTP).
Mount it in `src/app.ts`. Owner: see .github/CODEOWNERS and docs/team/squads.md.
