# interview module

Copy the pattern from `modules/ideas`: `interview.schema.ts` (zod, imported from @fi/contracts where shared) -> `interview.repo.ts` (SQL) -> `interview.service.ts` (rules) -> `interview.routes.ts` (HTTP).
Mount it in `src/app.ts`. Owner: see .github/CODEOWNERS and docs/team/squads.md.
