# radar module

Copy the pattern from `modules/ideas`: `radar.schema.ts` (zod, imported from @fi/contracts where shared) -> `radar.repo.ts` (SQL) -> `radar.service.ts` (rules) -> `radar.routes.ts` (HTTP).
Mount it in `src/app.ts`. Owner: see .github/CODEOWNERS and docs/team/squads.md.
