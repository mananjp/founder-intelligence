// Screen: Login / Signup (Bible §33) — Account creation and access.
// Owner: Frontend squad. Fetch via apiFetch() from '@/lib/apiClient'; types from '@fi/contracts'.
export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Login / Signup</h1>
      <p style={{ color: "var(--fi-muted)" }}>Account creation and access.</p>
    </main>
  );
}
