// Screen: Research Live (Bible §33) — Progress via SSE, without noisy internals.
// Owner: Frontend squad. Fetch via apiFetch() from '@/lib/apiClient'; types from '@fi/contracts'.
export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Research Live</h1>
      <p style={{ color: "var(--fi-muted)" }}>Progress via SSE, without noisy internals.</p>
    </main>
  );
}
