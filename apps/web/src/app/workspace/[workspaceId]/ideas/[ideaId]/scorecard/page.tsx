// Screen: Scorecard (Bible §33) — Opportunity dimensions + evidence coverage.
// Owner: Frontend squad. Fetch via apiFetch() from '@/lib/apiClient'; types from '@fi/contracts'.
export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Scorecard</h1>
      <p style={{ color: "var(--fi-muted)" }}>
        Opportunity dimensions + evidence coverage.
      </p>
    </main>
  );
}
