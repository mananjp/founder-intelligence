// Screen: Copilot (Bible §33) — Ask questions about the whole project.
// Owner: Frontend squad. Fetch via apiFetch() from '@/lib/apiClient'; types from '@fi/contracts'.
export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Copilot</h1>
      <p style={{ color: "var(--fi-muted)" }}>
        Ask questions about the whole project.
      </p>
    </main>
  );
}
