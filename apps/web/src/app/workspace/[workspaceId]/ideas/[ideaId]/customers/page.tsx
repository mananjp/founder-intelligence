// Screen: Customers (Bible §33) — ICP, pain, language, reviews, interviews.
// Owner: Frontend squad. Fetch via apiFetch() from '@/lib/apiClient'; types from '@fi/contracts'.
export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Customers</h1>
      <p style={{ color: "var(--fi-muted)" }}>ICP, pain, language, reviews, interviews.</p>
    </main>
  );
}
