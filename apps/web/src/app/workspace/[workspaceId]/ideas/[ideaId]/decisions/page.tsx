// Screen: Decision Trace (Bible §33) — Why a recommendation exists.
// Owner: Frontend squad. Fetch via apiFetch() from '@/lib/apiClient'; types from '@fi/contracts'.
export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Decision Trace</h1>
      <p style={{ color: "var(--fi-muted)" }}>Why a recommendation exists.</p>
    </main>
  );
}
