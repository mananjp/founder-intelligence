// Screen: Adaptive Interview (Bible §33) — Conversational discovery -> Research Brief.
// Owner: Frontend squad. Fetch via apiFetch() from '@/lib/apiClient'; types from '@fi/contracts'.
export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Adaptive Interview</h1>
      <p style={{ color: "var(--fi-muted)" }}>
        Conversational discovery → Research Brief.
      </p>
    </main>
  );
}
