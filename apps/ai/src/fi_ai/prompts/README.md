# Prompt registry
One YAML per prompt: `id`, `version`, `task`, `input_schema`, `output_schema`, `system`, `user_template`, `eval_suite`.
Prompts are code: PRs touching this folder require 2 reviews and a passing `make eval`.
